package org.example.HW2;

public class TwoNumbersSum {
    static boolean checkSum(int firstNumber, int secondNumber) {
        int result = firstNumber + secondNumber;
        return (10 <= result && result <= 20);
    }
}

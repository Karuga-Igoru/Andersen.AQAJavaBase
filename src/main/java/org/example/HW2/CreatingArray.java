package org.example.HW2;

public class CreatingArray {
    static int[] createArrayFrom(int len, int initialValue) {
        int[] array = new int[len];
        for (int i = 0; i < len; i++) {
            array[i] = initialValue;
        }
        return array;
    }
}
